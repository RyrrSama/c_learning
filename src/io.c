#include<stdio.h>

int main(){

    // puts("Enter the char : ");
    //int assci_code = getchar();
    // puts("Character entered was : ");
    // putchar(assci_code);
    // puts("");

    char name[25];
    //gets(name);
    scanf("%s", &name);
    puts("The entered username is : ");
    puts(name);

}